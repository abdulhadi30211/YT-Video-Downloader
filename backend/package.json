{
  "name": "yt-demo-backend",
  "version": "1.0.0",
  "description": "Backend server for YouTube video downloader using yt-dlp",
  "type": "module",
  "main": "src/api/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node src/api/server.js",
    "dev": "ts-node src/api/server.ts",
    "postinstall": "if [ \"$(uname)\" = \"Linux\" ]; then apt-get update && apt-get install -y python3-pip && pip3 install yt-dlp; fi || true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.22.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.25",
    "@types/node": "^22.19.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
